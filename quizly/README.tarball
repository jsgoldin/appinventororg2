# Here's the command used to create a tarball
# and exclude all hidden files:

tar -cv --exclude '.*' -f quizly-0304.tar quizly/ 

# For now, to distinguish versions, we use the month and
# day in the name of the tarball.




